%!TEX root = ../../../thesis.tex


 
\begin{table}
\tiny
% \left



\begin{tabu} to \linewidth { |[1pt middleGrey] l g g l@{\hspace*{6pt}} l@{\hspace*{6pt}} l g g l@{\hspace*{6pt}} l@{\hspace*{6pt}} l g g g l@{\hspace*{6pt}} l@{\hspace*{6pt}} g g|[1pt middleGrey]}

    \taburulecolor{middleGrey}\hline
    \tableHeaderStyle

    \textit{CONTROLS}& 
    \multicolumn{2}{g}{INIT.} & 
    \multicolumn{3}{g}{EXEMP.} &
    \multicolumn{2}{g}{PARAM.} & 
    \multicolumn{3}{g}{HAND.} & 
    \multicolumn{3}{g}{FILL.} & 
    \multicolumn{2}{g}{GUIDE.} & 
    \multicolumn{2}{g|[1pt middleGrey]}{PLACE.}
    \\

    & 
    \sidy{Configuration} & \sidy{Initialization} & 
    \sidy{Image} & \sidy{Arrangement} & \sidy{Element} &
    \sidy{Visual Output } & \sidy{System} &
    \sidy{Visualization} & \sidy{Image} & \sidy{Sketch} &
    \sidy{Shapes} & \sidy{Masking} & \sidy{Curve} &
    \sidy{Painting} & \sidy{Directions} &
    \sidy{Element} & \sidy{Drag \& Drop}
    \\

    \multicolumn{18}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{As Basis Elements}} \\

    % Reference
    \citet{anderson_2008_udt} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$&
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    &$\times$& $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
    $\times$ & 
    \\

    % Reference
    \citet{yu_2012_ans} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & &
    % PARAM:
    % Visual output
    % System/Generation
     & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    && $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\


    \citet{xu_2009_mcc} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & &
    % PARAM:
    % Visual output
    % System/Generation
     & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & & $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{merrell_2010_ecs} &
    % INIT:
    % Config & Task Init & 
     &  & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
     &  &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
     & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & $\times$
    \\

    \citet{zehnder_2016_dso} &
    % INIT:
    % Config & Task Init & 
     &  & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
     & $\times$ & 
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
    $\times$ & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
    $\times$ & $\times$
    \\

    \multicolumn{18}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{As Control Mechanism}} \\

    \citet{mech_2012_tdf}* &
    % INIT:
    % Config & Task Init & 
    $\times$ &  & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & & $\times$ &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & $\times$ & $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    $\times$ & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{jacobs_2018_dbe} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
    $\times$ & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
     & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    $\times$ & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
    $\times$ & 
    \\


    \citet{lu_2014_dds} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & & $\times$ &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
     & & $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\


    \citet{zhou_2014_tsv} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & & $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\


    \citet{kazi_2012_vit} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & & $\times$ &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & & $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    $\times$ &$\times$ &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
    $\times$ & 
    \\


    \citet{xing_2014_apr} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
    $\times$ & & $\times$ &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & & $\times$ &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
    $\times$ &$\times$ &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
    $\times$ & 
    \\
    \taburulecolor{middleGrey}\hline

\end{tabu}

\bigskip

\begin{tabu}{ |[1pt middleGrey] l g g g c c g g |[1pt middleGrey]}

    \taburulecolor{middleGrey}\hline
    \tableHeaderStyle

    \textit{CREATIVE MEANS}& 
    \multicolumn{3}{g}{NAVI.} & 
    \multicolumn{2}{g}{TRANS.} &
    \multicolumn{2}{g|[1pt middleGrey]}{VARI.}  
    \\

    & 
    \sidy{Interactive} & \sidy{\newl{Control}{Quantity}} & \sidy{\newl{Navigation}{History}} & 
    \sidy{\newl{Control}{Domain}} & \sidy{\newl{Control}{Communication}} &
    \sidy{\newl{Design Space}{Size}} & \sidy{\newl{Design Space}{Openness}}
    \\

    \multicolumn{8}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{As Basis Elements}} \\

    \citet{anderson_2008_udt} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
     & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    \\

    \citet{yu_2012_ans} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ &  &  &
    % TRANS:
    % Quality of controls
    % Control Communication
     & &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    \\


    \citet{xu_2009_mcc} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$  & &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    \\

    \citet{merrell_2010_ecs} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     &  &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ &  &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    \\

    \citet{zehnder_2016_dso} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\bullet$ &
    % VARI:
    % Size design space
    % Openness of design space
     & $\circ$ 
    \\


    \multicolumn{8}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{As Control Mechanism}} \\

    \citet{mech_2012_tdf} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\bullet$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
     & $\bullet$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\bullet$ & $\circ$ 
    
    \\

    \citet{jacobs_2018_dbe} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\bullet$ & $\bullet$ 
    \\

    \citet{lu_2014_dds} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\circ$ &$\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ & 
    \\

    \citet{zhou_2014_tsv} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     $\circ$ &  $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
      & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    \\

    \citet{kazi_2012_vit} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\bullet$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\bullet$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\bullet$ & $\bullet$ 
    \\

    \citet{xing_2014_apr} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\bullet$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\bullet$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\bullet$ & $\circ$ 
    \\
    \taburulecolor{middleGrey}\hline

\end{tabu}
\hfill\



\label{table:taxo_curve}
\caption[Interaction and creative means for techniques with curves and sketches]{Interaction and creative means for techniques with curves and sketches as visual elements and underlying control mechanism. $^{*}$Please note that \citet{mech_2012_tdf} present a procedural modeling \textit{engine}, which in principle can be programmed to include almost any control type.}
\end{table}
    

