%!TEX root = ../../../thesis.tex


 
\begin{table}
\tiny
% \left



\begin{tabu} to \linewidth { |[1pt middleGrey] l g g l@{\hspace*{6pt}} l@{\hspace*{6pt}} l g g l@{\hspace*{6pt}} l@{\hspace*{6pt}} l g g g l@{\hspace*{6pt}} l@{\hspace*{6pt}} g g|[1pt middleGrey]}

    \taburulecolor{middleGrey}\hline
    \tableHeaderStyle

    \textit{CONTROLS}& 
    \multicolumn{2}{g}{INIT.} & 
    \multicolumn{3}{g}{EXEMP.} &
    \multicolumn{2}{g}{PARAM.} & 
    \multicolumn{3}{g}{HAND.} & 
    \multicolumn{3}{g}{FILL.} & 
    \multicolumn{2}{g}{GUIDE.} & 
    \multicolumn{2}{g|[1pt middleGrey]}{PLACE.}
    \\

    & 
    \sidy{Configuration} & \sidy{Initialization} & 
    \sidy{Image} & \sidy{Arrangement} & \sidy{Element} &
    \sidy{Visual Output } & \sidy{System} &
    \sidy{Visualization} & \sidy{Image} & \sidy{Sketch} &
    \sidy{Shapes} & \sidy{Masking} & \sidy{Curve} &
    \sidy{Painting} & \sidy{Directions} &
    \sidy{Element} & \sidy{Drag \& Drop}
    \\

    \multicolumn{18}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Stochastic}} \\

    \citet{lagae_2010_pis} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{galerne_2012_gne} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
    $\times$ & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{galerne_2017_tno} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{gilet_2012_mkn} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\
    

    \citet{gilet_2014_lrn} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{pavie_2016_pts} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\
    
    \citet{guingo_2017_btm} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & & 
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\
    
    \citet{kang_2017_fpt} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\
    
    \citet{gilet_2010_ias} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & & $\times$ &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\
    
    \multicolumn{18}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Unrestricted Designs}} \\

    \citet{lefebvre_2000_ass} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\



    \citet{gilet_2012_map} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & $\times$ & $\times$ &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\



    \citet{bourque_2004_ptm} &
    % INIT:
    % Config & Task Init & 
    $\times$ & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
    $\times$ & & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\


    \multicolumn{18}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Element Arrangements}} \\

    \citet{barla_2006_spa} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & $\times$ & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
     & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{hurtut_2009_ags} &
    % INIT:
    % Config & Task Init & 
     & & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & $\times$ & &
    % PARAM:
    % Visual output
    % System/Generation
     & &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & $\times$ &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{ijiri_2008_aeb} &
    % Config & Task Init & 
     & & 
    % Image
    % Element arrangement
    % Element
     & $\times$ &  &
    % Visual output
    % System/Generation
    $\times$ & $\times$ & 
    % Custom visual UI
    % Image-based
    % Sketch-based
    &  &  &
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & $\times$ &  &
    % Painting/Strokes to follow
    % Directions
    $\times$ & $\times$ & 
    % Element Placement
    % Element Drag&Drop
     & 
    \\

    \citet{ma_2011_det} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & $\times$ & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$  & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & $\times$ & 
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & $\times$
    \\



    \multicolumn{18}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Grammar Generation}} \\

    \citet{stava_2010_ipm} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & $\times$ & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\


    \citet{talton_2012_ldp} &
    % INIT:
    % Config & Task Init & 
     & $\times$ & 
    % EXEMP:
    % Image
    % Element arrangement
    % Element
     & $\times$ & &
    % PARAM:
    % Visual output
    % System/Generation
    $\times$ & $\times$ &
    % CONTR:
    % Custom visual UI
    % Image-based
    % Sketch-based
     & &  &
    % FILL:
    % Shapes
    % Masking
    % Curve/Strokes to fill
    $\times$ & &  &
    % GUIDE:
    % Painting/Strokes to follow
    % Directions
     & &
    % PLACE:
    % Element Placement
    % Element Drag&Drop
     & 
    \\
    \taburulecolor{middleGrey}\hline

\end{tabu}
\hfill\


% %-------------------------------------------------------------------------
% %-------------------------------------------------------------------------

% %-------------------------------------------------------------------------
% %-------------------------------------------------------------------------

% %-------------------------------------------------------------------------
% %-------------------------------------------------------------------------








\label{table:taxo_example_interaction}
\caption[Interaction methods for example-based techniques]{Interaction means for example-based techniques.}
\end{table}



\begin{table}
\tiny
% \left


\begin{tabu} to \linewidth { |[1pt middleGrey] l g g g c c g g |[1pt middleGrey]}

    \taburulecolor{middleGrey}\hline
    \tableHeaderStyle

     \textit{CREATIVE MEANS}& 
    \multicolumn{3}{g}{NAVI.} & 
    \multicolumn{2}{g}{TRANS.} &
    \multicolumn{2}{g}{VARI.} 
    \\

    & 
    \sidy{Interactive} & \sidy{\newl{Control}{Quantity}} & \sidy{\newl{Navigation}{History}} & 
    \sidy{\newl{Control}{Domain}} & \sidy{\newl{Control}{Communication}} &
    \sidy{\newl{Design Space}{Size}} & \sidy{\newl{Design Space}{Openness}} 
    \\

    \multicolumn{8}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Stochastic}} \\

    \citet{lagae_2010_pis} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & &
    % VARI:
    % Size design space
    % Openness of design space
     &   
    
    \\

    \citet{galerne_2012_gne} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     & &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    
    \\

    \citet{galerne_2017_tno} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & &
    % VARI:
    % Size design space
    % Openness of design space
     & 
    
    \\

    \citet{gilet_2012_mkn} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     $\circ$& $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ &  
    
    \\

    \citet{gilet_2014_lrn} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    ? & $\circ$ & &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ &  
    
    \\

    \citet{pavie_2016_pts} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\circ$&
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ & 
    
    \\

    \citet{guingo_2017_btm} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ & 
    
    \\

    \citet{kang_2017_fpt} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\circ$ & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\bullet$ &  
    
    \\

    \citet{gilet_2010_ias} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ &  $\circ$&
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ & 
    
    \\


    \multicolumn{8}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Unrestricted Designs}} \\

    \citet{lefebvre_2000_ass} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & $\circ$&
    % VARI:
    % Size design space
    % Openness of design space
     &   
    
    \\

    \citet{gilet_2012_map} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$&  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & $\circ$&
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ &   
    
    \\

    \citet{bourque_2004_ptm} & 
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     & $\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$& &
    % VARI:
    % Size design space
    % Openness of design space
    $\bullet$ &   
    
    \\



    \multicolumn{8}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Element Arrangements}} \\

    \citet{barla_2006_spa} &
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\circ$ & &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & &
    % VARI:
    % Size design space
    % Openness of design space
     & $\circ$ 
    
    \\

    \citet{hurtut_2009_ags} &
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\bullet$ & $\circ$ &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ & 
    
    \\

    \citet{ijiri_2008_aeb} &
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\bullet$ & $\circ$ &  &
    % Quality of controls
    % Control Communication
    $\circ$ & $\bullet$ & 
    % Size design space
    % Openness of design space
    $\bullet$ & $\circ$  

    \\

    \citet{ma_2011_det} &
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\circ$ & $\circ$&  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$& $\circ$&
    % VARI:
    % Size design space
    % Openness of design space
     $\bullet$ & $\circ$
    
    \\



    \multicolumn{8}{|[1pt middleGrey]l|[1pt middleGrey]}{\textit{Grammar Generation}} \\

    \citet{stava_2010_ipm} &
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
    $\circ$ & $\circ$&  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ & 
    
    \\


    \citet{talton_2012_ldp} &
    % NAVI:
    % Interactive 
    % Quantity of Controls
    % Navigation History 
     &$\circ$ &  &
    % TRANS:
    % Quality of controls
    % Control Communication
    $\circ$ & &
    % VARI:
    % Size design space
    % Openness of design space
    $\circ$ &   
    
    \\

    \taburulecolor{middleGrey}\hline

\end{tabu}
\hfill\




\caption[Creative means for example-based techniques]{Creative means for example-based techniques. Please note that performance times consider the whole creation processes for an artist, including the example matching and possible parameter interaction (for noise generation techniques usually only the noise evaluation times are considered).
% Mention when a model is a configuration step and when not?
}
\label{table:taxo_example_creative}
\end{table}
    

